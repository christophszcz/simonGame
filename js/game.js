var colors = ['red', 'yellow', 'blue', 'yellow'], number, collection = [];
 
function randomNumber(){
	number = Math.floor(Math.random() * 4);
	console.log(randomNumber);
}

randomNumber();
console.log(number);